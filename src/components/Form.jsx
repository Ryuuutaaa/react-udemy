const Form = () => {
  const hadnleChange = () => {};
  return (
    <div>
      <form action="">
        <input name="course" value="" onChange={hadnleChange} />
      </form>
    </div>
  );
};

export default Form;
