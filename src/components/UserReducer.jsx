import React, { useReducer } from "react";

const UserReducer = () => {
  return (
    <div>
      <button>+</button>
      <p>Counter - 0</p>
      <button>-</button>
    </div>
  );
};

export default UserReducer;
